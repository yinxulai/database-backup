{{/*
Congratulations! You have installed the database-backup chart.
*/}}
{{- if .Release.IsInstall }}

## Next Steps

1. **Configure your backup** by setting `.Values.config.content` with your BackupGroup YAML:

   ```bash
   # Edit values file
   helm show values . > values.yaml
   # Then edit values.yaml with your config
   ```

2. **Create required Kubernetes secrets**:

   ```bash
   # Database password secret
   kubectl create secret generic postgres-secret \
     --from-literal=password=YOUR_PASSWORD

   # AWS credentials secret
   kubectl create secret generic aws-secret \
     --from-literal=access-key-id=YOUR_KEY \
     --from-literal=secret-access-key=YOUR_SECRET
   ```

3. **Upgrade with your configuration**:

   ```bash
   helm upgrade --install database-backup . \
     --set config.content="$(cat <<'EOF'
   apiVersion: backup.taicode/v1
   kind: BackupGroup
   metadata:
     name: my-backup
   spec:
     source:
       type: postgresql
       connection:
         host: postgres.database.svc.cluster.local
         port: 5432
         username: postgres
         passwordSecretRef:
           type: k8s
           secretName: postgres-secret
           secretKey: password
       database: myapp
     destination:
       type: s3
       s3:
         endpoint: https://s3.amazonaws.com
         region: us-east-1
         bucket: my-backups
         accessKeySecretRef:
           type: k8s
           secretName: aws-secret
           secretKey: access-key-id
         secretKeySecretRef:
           type: k8s
           secretName: aws-secret
           secretKey: secret-access-key
   EOF
   )"
   ```

{{- else if .Release.IsUpgrade }}

## Database Backup Upgraded!

{{- end }}

## CronJob Details

| Setting | Value |
|---------|-------|
| Schedule | `{{ .Values.schedule }}` ({{ .Values.timezone }}) |
| Concurrency | `{{ .Values.concurrencyPolicy }}` |
| Successful Jobs History | `{{ .Values.successfulJobsHistoryLimit }}` |
| Failed Jobs History | `{{ .Values.failedJobsHistoryLimit }}` |
| Suspend | `{{ .Values.suspend }}` |

## Useful Commands

```bash
# View CronJob
kubectl get cronjob {{ include "database-backup.fullname" . }}

# List all CronJobs
kubectl get cronjob -l app.kubernetes.io/name=database-backup

# Manually trigger a backup
kubectl create job --from=cronjob/{{ include "database-backup.fullname" . }} manual-backup-$(date +%s)

# View ConfigMap
kubectl get configmap {{ include "database-backup.fullname" . }}

# View logs of last job
kubectl logs -l job-name={{ include "database-backup.fullname" . }} --tail=100

# Uninstall
helm uninstall {{ .Release.Name }}
```

{{- if not .Values.config.content }}

## ⚠️ Warning: No backup configuration!

You have not set `.Values.config.content`. The CronJob is installed but will not perform any backup until configured.

Set `config.content` with your BackupGroup YAML to enable backups.

{{- end }}
